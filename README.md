# tourney-site-test-template-v2

# To Run
* Build the image:
        
        $ ./docker-build.sh
    
* Run the containers:
        
        $ ./docker-run.sh

Now the app will be running on localhost:5000
